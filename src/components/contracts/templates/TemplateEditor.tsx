"use client";

// Stub — Phase 8b will replace this with the full editor component.
export function TemplateEditor({ templateId }: { templateId: string }) {
  return <div>Loading template {templateId}...</div>;
}
